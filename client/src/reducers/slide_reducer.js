import types from '../actions/types';

const DEFAULT_STATE = {

};

export default funcitong(state = DEFAULT_STATE, action) {
    switch (action.type) {
        default:
            return state;
    }
}